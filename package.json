{
  "name": "boot-anatomy",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Interactive 3D walkthrough of a desktop PC boot sequence, from power button to login screen.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/three": "^0.180.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
